# Storage and Database

Browser IndexedDB is suitable for local testing, but it is device-specific and browser-managed. Production audio should use S3-compatible storage such as Cloudflare R2, Backblaze B2, Amazon S3, Supabase Storage, or Firebase Storage. Upload with short-lived signed URLs generated by your server. Store only file metadata and permissions in the database.
