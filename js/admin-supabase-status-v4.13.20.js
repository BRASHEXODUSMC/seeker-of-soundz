(()=>{'use strict';const panel=document.getElementById('adminPanel');if(!panel)return;const live=['Members, roles & access','Forums, replies & reactions','Forum categories, tags & presets','Collaboration projects, messages & files','Profiles, ranks & avatars'];function add(){if(!panel.textContent.includes('Admin overview')&&!panel.querySelector('.overviewGrid'))return;if(panel.querySelector('.supabaseSyncOverview'))return;const card=document.createElement('section');card.className='dashboardCard supabaseSyncOverview';card.innerHTML=`<p class="sectionEyebrow">Production data status</p><h2>Supabase synchronization</h2><p>Core community and collaboration systems are connected to Supabase. Admin sections that still use preview data are identified below rather than being presented as live database data.</p><div class="supabaseSyncGrid">${live.map(x=>`<span>✓ ${x}</span>`).join('')}</div><small>Announcements, music and gallery have Supabase database foundations, while their older Admin editors still require a dedicated migration pass. Merch, videos, events, visual preferences and performance diagnostics remain browser-side for now.</small>`;(panel.querySelector('.overviewGrid')||panel).appendChild(card)}new MutationObserver(add).observe(panel,{childList:true,subtree:true});add()})();
