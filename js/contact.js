(()=>{"use strict";const f=document.getElementById('contactForm');if(!f)return;const requestedType=new URLSearchParams(location.search).get('type');if(requestedType&&f.elements.type){f.elements.type.value=requestedType;if(requestedType==='Collaboration'){f.elements.subject.value=f.elements.subject.value||'Collaboration Studio access request';}}f.onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(f));d.id=crypto.randomUUID();d.date=new Date().toISOString();d.status='new';const messages=SOS.read(SOS.K.messages,[]);messages.push(d);SOS.write(SOS.K.messages,messages);const cfg=window.SOS_BACKEND||{};if(cfg.formspreeEndpoint){try{await fetch(cfg.formspreeEndpoint,{method:'POST',headers:{'Accept':'application/json'},body:new FormData(f)})}catch{}}document.getElementById('contactStatus').textContent='Message received and saved to the admin inbox.';f.reset();SOS.toast('Message sent')};})();