(()=>{"use strict";const featured=document.querySelector('.featuredEventContent');if(featured&&!featured.querySelector('.eventCountdown')){const wrap=document.createElement('div');wrap.className='eventCountdown';const target=new Date();target.setDate(target.getDate()+30);const render=()=>{let d=Math.max(0,target-Date.now());const days=Math.floor(d/86400000);const hours=Math.floor(d/3600000)%24;const mins=Math.floor(d/60000)%60;const secs=Math.floor(d/1000)%60;wrap.innerHTML=`<span><strong>${days}</strong><small> Days</small></span><span><strong>${String(hours).padStart(2,'0')}</strong><small> Hrs</small></span><span><strong>${String(mins).padStart(2,'0')}</strong><small> Min</small></span><span><strong>${String(secs).padStart(2,'0')}</strong><small> Sec</small></span>`};featured.querySelector('.eventActions')?.before(wrap);render();setInterval(render,1000)}document.querySelectorAll('.eventActions a[href="#"]').forEach(a=>a.addEventListener('click',e=>e.preventDefault()))})();